import { ConditionalMenuDirective } from './conditional-menu.directive';

describe('ConditionalMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalMenuDirective();
    expect(directive).toBeTruthy();
  });
});
