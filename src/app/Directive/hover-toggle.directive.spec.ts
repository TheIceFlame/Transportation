import { HoverToggleDirective } from './hover-toggle.directive';

describe('HoverToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverToggleDirective();
    expect(directive).toBeTruthy();
  });
});
